---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "donetick.fullname" . }}-data-pvc
  namespace: {{ .Values.namespace }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.persistence.dataStorage }}
  storageClassName: {{ .Values.persistence.storageClassName }}
