{{ include "homelab-common.tailscale-ingress" (dict "Values" .Values "fullname" (include "immich.fullname" .) "labels" (include "immich.labels" . | trim) "namespace" .Values.namespace "servicePort" .Values.server.service.port) }}
